import { createFileRoute, Link } from "@tanstack/react-router";
import { MapPin, SlidersHorizontal, Sparkles, ShieldCheck } from "lucide-react";
import { ClienteApp } from "@/features/cliente/ClienteApp";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "InmoPilot AI — Mapa inmobiliario inteligente" },
      {
        name: "description",
        content:
          "Copiloto inmobiliario con IA: mapa interactivo, Match Score, valuación dinámica y riesgo documental antes de firmar.",
      },
      { property: "og:title", content: "InmoPilot AI" },
      {
        property: "og:description",
        content:
          "El mapa responde dónde buscar. La IA responde cuál conviene y por qué.",
      },
    ],
  }),
  component: Landing,
});

const benefits = [
  {
    icon: MapPin,
    title: "Búsqueda por mapa interactivo",
    desc: "Explora La Paz con pines inteligentes por zona.",
  },
  {
    icon: SlidersHorizontal,
    title: "Filtros inteligentes",
    desc: "Precio, zona, habitaciones, baños y más.",
  },
  {
    icon: Sparkles,
    title: "Match Score cliente-propiedad",
    desc: "Sabes al instante qué tan bien encaja cada inmueble.",
  },
  {
    icon: ShieldCheck,
    title: "Valuación y riesgo antes de firmar",
    desc: "Precio justo y semáforo documental con IA.",
  },
];

function Landing() {
  return (
    <div className="min-h-screen bg-background">

      {/* HEADER */}
      <header className="border-b border-border bg-navy text-navy-foreground">
        <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
          
          <div className="flex items-center gap-2 font-semibold tracking-tight">
            <div className="grid h-8 w-8 place-items-center rounded-lg bg-navy-foreground/15">
              <MapPin className="h-4 w-4" />
            </div>
            InmoPilot AI
          </div>

          <nav className="hidden items-center gap-6 text-sm opacity-90 md:flex">
            <a href="#">Inicio</a>
            <a href="#quienes">Quiénes somos</a>
            <a href="#explorar">Explorar propiedades</a>
            <a href="/agente">Acceso agente</a>
          </nav>

          <div className="hidden items-center gap-3 md:flex">
            <button
              onClick={() =>
                window.dispatchEvent(new Event("open-register"))
              }
              className="rounded-md border border-border bg-background px-3 py-1 text-sm"
            >
              Registrarme
            </button>

            <Link to="/cliente" className="text-sm hover:underline">
              Iniciar sesión
            </Link>
          </div>
        </div>
      </header>

      {/* HERO */}
      <section className="mx-auto max-w-6xl px-6 py-16 text-center">

        <div className="inline-flex items-center gap-2 rounded-full border border-border bg-secondary px-4 py-1.5 text-xs font-medium text-secondary-foreground">
          <Sparkles className="h-3.5 w-3.5" />
          Demo Proptech · Hackathon
        </div>

        <h1 className="mt-6 text-4xl font-bold tracking-tight text-navy md:text-5xl">
          Encuentra la propiedad correcta con un mapa inteligente
        </h1>

        <p className="mx-auto mt-4 max-w-2xl text-lg text-muted-foreground">
          Explora propiedades por zona, precio, habitaciones y baños. Regístrate
          para activar recomendaciones personalizadas.
        </p>

        <div className="mt-6 flex justify-center gap-3">
          <a
            href="#explorar"
            className="rounded-md bg-navy px-4 py-2 text-sm text-navy-foreground"
          >
            Explorar propiedades
          </a>

          <button
            onClick={() =>
              window.dispatchEvent(new Event("open-register"))
            }
            className="rounded-md border border-border px-4 py-2 text-sm"
          >
            Registrarme
          </button>
        </div>
      </section>

      {/* MAPA / APP */}
      <div id="explorar" className="mx-auto max-w-7xl px-6">
        <ClienteApp showTopHeader={false} />
      </div>

      {/* QUIÉNES SOMOS */}
      <section id="quienes" className="border-t border-border bg-secondary/40">
        <div className="mx-auto max-w-6xl px-6 py-16 text-center">
          <h2 className="text-3xl font-bold">Quiénes somos</h2>
          <p className="mt-4 text-muted-foreground">
            InmoPilot AI es una plataforma proptech que combina mapas inteligentes
            e inteligencia artificial para ayudar a encontrar propiedades con
            mayor precisión y menor riesgo.
          </p>
        </div>
      </section>

      {/* QUÉ OFRECEMOS */}
      <section className="border-t border-border bg-secondary/40">
        <div className="mx-auto max-w-6xl px-6 py-16">
          
          <h2 className="text-center text-3xl font-bold">
            Qué ofrecemos
          </h2>

          <p className="mt-3 text-center text-muted-foreground">
            Tecnología inmobiliaria con IA para decisiones más rápidas y seguras.
          </p>

          <div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-4">
            {benefits.map(({ icon: Icon, title, desc }) => (
              <div
                key={title}
                className="rounded-2xl border border-border bg-card p-6 shadow-sm"
              >
                <div className="grid h-10 w-10 place-items-center rounded-lg bg-navy text-navy-foreground">
                  <Icon className="h-5 w-5" />
                </div>
                <h3 className="mt-4 font-semibold">{title}</h3>
                <p className="mt-1 text-sm text-muted-foreground">{desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="border-t border-border py-8 text-center text-xs text-muted-foreground">
        InmoPilot AI · Demo simulada — La Paz, Bolivia
      </footer>

    </div>
  );
}
